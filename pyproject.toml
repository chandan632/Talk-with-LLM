[project]
name = "talk-with-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chandan632", email = "chandanrout737@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "groq>=1.7.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
talk-with-llm = "talk_with_llm:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
